# deps
node_modules/
**/node_modules/

# build
dist/
build/
**/dist/

# secrets & local data (CRITICAL — never commit)
.env
.env.*
!.env.example
*.sqlite
*.sqlite3
*.db
backend/data/
backend/uploads/
# Naskah skripsi yang dipakai untuk uji coba. Repo ini publik dan naskahnya
# milik penulisnya, bukan milik proyek — satu `git add .` sudah cukup untuk
# menerbitkannya secara permanen.
*.pdf
!backend/test/fixtures/*.pdf

# misc
.DS_Store
*.log

# build output dari tsc test build
backend/dist-test/
